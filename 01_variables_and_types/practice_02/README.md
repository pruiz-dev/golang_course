# Практика 02: Ловушка Shadowing

## Задание

Твоя задача — починить баг с затенением переменной в программе.

### Шаги:
1. Создай папку `practice_02`, если еще не создал, и инициализируй в ней модуль `go mod init practice_02`.
2. Создай файл `main.go`.
3. Скопируй в него следующий код:

```go
package main

import (
	"fmt"
)

// Имитируем запрос к базе данных
func fetchUser() (string, error) {
	return "Dmitry", nil
}

// Имитируем получение возраста и статуса
func getStats() (int, bool) {
    return 25, true
}

func main() {
	var currentUser string

	// ЧАСТЬ 1: Баг с затенением (Shadowing)
	isReady := true
	if isReady {
		// ВНИМАНИЕ: Здесь закрался баг с затенением!
		currentUser, err := fetchUser()
		if err != nil {
			fmt.Println("Ошибка:", err)
			return
		}
		fmt.Println("Пользователь загружен локально:", currentUser)
	}

    // ЧАСТЬ 2: Множественное присваивание
    // У нас уже есть переменные age и isActive, но мы хотим их обновить.
    age, isActive := 20, false
    
    // Твоя задача: правильно использовать функцию getStats(), чтобы обновить age, 
    // создать НОВУЮ переменную newStatus, и НЕ использовать ключевое слово var.
    
    // ДОПИШИ КОД ЗДЕСЬ

	// Выводы для проверки
	fmt.Printf("Финальный результат: пользователь '%s' авторизован.\n", currentUser)
    fmt.Printf("Возраст: %d, Статус: %t\n", age, isActive)
}
```

4. Твоя задача: 
   - В Части 1 изменить код так, чтобы переменная `currentUser` из внешней области видимости получила правильное значение (избавиться от затенения внутри `if`). 
   - В Части 2 продемонстрировать понимание исключения оператора `:=`. Используя функцию `getStats()`, обнови переменную `age` и создай новую переменную `newStatus` в одну строку. Выведи `newStatus` в консоль.
5. Убедись, что программа работает корректно через `go run .`. Ожидается, что имя будет `Dmitry`, а возраст `25`.

Жду исправленный код! Мы на пути к уровню Middle, так что привыкаем решать такие задачи самостоятельно.
